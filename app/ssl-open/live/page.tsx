import OpenClient from './OpenClient'

export const metadata = { title: 'SSL Open 2026 Results — Live Leaderboard', robots: { index: false } }
export default function OpenLivePage() {
  return <OpenClient />
}
